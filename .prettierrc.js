module.exports = {
  /* Ширина строки больше стандартной (80), т.к. используются 4 пробела вместо 2. */
  printWidth: 120,
  /* Отдаётся предпочтение одинарным кавычкам. */
  singleQuote: true,
  /* Tab равен 2 пробелам. */
  tabWidth: 2,
  /* Запятые после последнего элемента ставятся везде кроме аргументов функций (ES2017). */
  trailingComma: 'es5',
  /* Конец строки для Unix окружения */
  endOfLine: 'lf',
};
